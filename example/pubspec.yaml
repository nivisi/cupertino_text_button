name: example
description: An example how to use the cupertino_text_button package.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.3
  cupertino_text_button:
    path: ../
  flutter:
    sdk: flutter

dev_dependencies: null

flutter:
  uses-material-design: true
