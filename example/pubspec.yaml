name: example
description: An example how to use the cupertino_text_button paclage.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.3
  cupertino_text_button:
    path: ../
  flutter:
    sdk: flutter

dev_dependencies: null

flutter:
  uses-material-design: true
